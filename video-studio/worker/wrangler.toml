name = "video-studio-proxy"
main = "cors-proxy.js"
compatibility_date = "2025-01-01"

# Restrict the proxy to your own page:
#   wrangler secret put ALLOWED_ORIGIN
# ...or uncomment and set it in the clear (it is not a secret, just an allowlist):
# [vars]
# ALLOWED_ORIGIN = "https://marsminhas423.github.io"
